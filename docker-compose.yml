version: '3.8'

services:
  asinu-backend:
    build: .
    container_name: asinu-backend
    ports:
      - '3300:3300'
    env_file:
      - .env
    networks:
      - asinu_net
    restart: unless-stopped

networks:
  asinu_net:
    external: true
